import React from "react";

export default function Explanation() {
  return (
    <div>
      <p>
        You could get a secure password generated and stored in a manager. But
        that's not always convenient. Here, all you need to do is remember the
        keyword, remember your salt word, and you can retrieve that
        auto-generated, sha256-hashed and salted, all-requirements-meeting
        secure code any time!
      </p>
    </div>
  );
}
